<ui:FluentWindow
    x:Class="SecAudit.App.Views.Shell.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:pages="clr-namespace:SecAudit.App.Views.Pages"
    Title="SecAudit"
    Icon="pack://application:,,,/Assets/sonla-logo.png"
    Width="1280" Height="800"
    MinWidth="960" MinHeight="600"
    WindowBackdropType="Mica"
    ExtendsContentIntoTitleBar="True"
    WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="SecAudit — Authorized Use Only">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="pack://application:,,,/Assets/sonla-logo.png" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <ui:NavigationView
            x:Name="RootNavigation"
            Grid.Row="1"
            PaneDisplayMode="Left"
            IsBackButtonVisible="Collapsed"
            OpenPaneLength="220">
            <ui:NavigationView.MenuItems>
                <ui:NavigationViewItem
                    Content="Dashboard"
                    TargetPageType="{x:Type pages:DashboardPage}">
                    <ui:NavigationViewItem.Icon>
                        <ui:SymbolIcon Symbol="Home24" />
                    </ui:NavigationViewItem.Icon>
                </ui:NavigationViewItem>
                <ui:NavigationViewItem
                    Content="Log Forensics"
                    TargetPageType="{x:Type pages:LogForensicsPage}">
                    <ui:NavigationViewItem.Icon>
                        <ui:SymbolIcon Symbol="DocumentSearch24" />
                    </ui:NavigationViewItem.Icon>
                </ui:NavigationViewItem>
            </ui:NavigationView.MenuItems>
            <ui:NavigationView.FooterMenuItems>
                <ui:NavigationViewItem
                    Content="Settings"
                    TargetPageType="{x:Type pages:SettingsPage}">
                    <ui:NavigationViewItem.Icon>
                        <ui:SymbolIcon Symbol="Settings24" />
                    </ui:NavigationViewItem.Icon>
                </ui:NavigationViewItem>
            </ui:NavigationView.FooterMenuItems>
        </ui:NavigationView>

        <!-- Footer: copyright + banner "Authorized Use Only".
             Logo tròn 20px bên trái, text chia 2 dòng: dòng 1 copyright,
             dòng 2 phiên bản + cảnh báo truy cập hợp pháp. -->
        <Grid Grid.Row="2" Margin="16,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Image
                Grid.Column="0"
                Source="pack://application:,,,/Assets/sonla-logo.png"
                Width="22" Height="22"
                Margin="0,0,8,0"
                VerticalAlignment="Center"
                RenderOptions.BitmapScalingMode="HighQuality" />
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <ui:TextBlock
                    FontSize="11"
                    FontWeight="SemiBold"
                    Opacity="0.85"
                    Text="Bản quyền thuộc về Mr. Phương — Phòng An ninh mạng, Công an tỉnh Sơn La" />
                <ui:TextBlock
                    FontSize="10"
                    Opacity="0.6"
                    Text="AUTHORIZED USE ONLY · SecAudit v0.1 · © 2026" />
            </StackPanel>
        </Grid>
    </Grid>
</ui:FluentWindow>
